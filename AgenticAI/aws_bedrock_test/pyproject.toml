[project]
name = "aws-bedrock-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain-aws>=1.4.1",
    "python-dotenv>=1.2.2",
]
